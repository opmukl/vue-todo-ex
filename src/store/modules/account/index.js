import Vue from './node_modules/vue';
import api from './node_modules/@/api/memos';

const state = {
  user: {}
};

const actions = {
  loginCheck() {}
};

const getters = {};

const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
